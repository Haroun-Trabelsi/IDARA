# ComplexityPipeline environment variables
# Copy this file to ".env" and fill in values as needed.

# MongoDB connection string (used by config.yaml via ${MONGODB_URI})
# Example for local Mongo:
MONGODB_URI=mongodb://localhost:27017/vfx_classification_demo

# Optional API key to protect FastAPI endpoints.
# Leave empty to disable API-key checks.
API_KEY=

# Note:
# - Deletion behavior is configured in config.yaml:
#     watchdog.delete_after_processing: true
#     watchdog.delete_zip_after_extract: true
#   These are enabled by default and do not require env variables.
# - Docker compose (docker-compose.yml) is configured to load ".env".
# - Ensure input_files/ exists (compose mounts ./input_files:/app/input_files).
