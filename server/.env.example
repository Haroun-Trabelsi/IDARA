# Copy this file to ".env" and fill in the values. Never commit real secrets to git.

# App
PORT=8080
APP_URL=http://localhost:8080

# CORS / Frontend
# Comma-separated list of allowed origins for CORS (e.g., https://app.example.com,https://admin.example.com)
CORS_ORIGINS=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# JWT
# Generate a strong random secret in production (e.g., `openssl rand -base64 48`)
JWT_SECRET=CHANGE_ME_SECURE_RANDOM

# MongoDB (choose either Atlas URI or local connection string)
# Example Atlas URI: mongodb+srv://USER:PASS@CLUSTER_NAME.mongodb.net/DB_NAME
MONGO_URI=mongodb+srv://USERNAME:PASSWORD@HOST/DB_NAME

# Email (Nodemailer)
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_app_password_or_secret

# ftrack API (https://www.ftrack.com/)
FTRACK_SERVER_URL=https://YOUR_SUBDOMAIN.ftrackapp.com
FTRACK_USERNAME=your_ftrack_username@example.com
FTRACK_API_KEY=your_ftrack_api_key_here
